# Product-owned crates that carry semantic/computational authority.
# The adapter's cfg(wasm)-only facade is target-incompatible with native
# cargo-mutants; its delegate is unit-tested and its export is browser-tested.
# The runtime exclusions match exactly two equivalent `&&` mutants. With the
# enforced one-workspace cache bound, the requested workspace is absent from
# both the map and LRU before eviction. With accepted materialization, a role
# assignment's stable ID and selected role are a single inseparable identity.
# Four exact loop-counter mutants are also excluded: cargo-mutants already
# detected them by timeout, but reports any timeout as a failed gate. Exact
# output tests retain the ordinary decrement mutations. `cargo mutants --list`
# proves no other runtime mutant matches these file/column/operator expressions.
# Multiple exact exclusion expressions are comma-separated.
# The kernel's contract-export binary is exercised by the contract drift gate;
# cargo test cannot call its `main` in a mutant sandbox. The matcher's optional
# Python-only facade is outside the browser product and creates 703,742 bogus
# tuple-return mutants; its Rust core remains fully in scope.
# Optional fields after the exclusion regex ratchet line, region, and function
# coverage for an established authority below the shared new-code defaults.
# The final field lists product features required by both coverage and mutation;
# omitting `incremental-v2` would silently test the retired non-authoritative
# kernel build instead of the production 55-step query engine.
# A seventh `no-default-features` field keeps a gate on the exact feature set
# linked into the browser when a crate's defaults only expose another product.
#
# Kernel exclusions, one justification per expression, all verified against
# `cargo mutants --list` so no other mutant matches:
# - count_duplicate_groups `> 1` -> `>= 1`: a run length is at least 1 at both
#   sites and a run of one adds `len - 1`, which is zero.
# - unalign_duplicate_timestamps `rows[i - 1]` -> `rows[i / 1]`: the scan then
#   compares a row to itself, so `has_dupes` is always true and the early
#   return is skipped; on rows with no tie the block loop rewrites nothing.
# - unalign_duplicate_timestamps `start < len` -> `<=`: the extra iteration
#   finds a run of one, indexes nothing, and writes nothing.
# - unalign_duplicate_timestamps `end = start + 1` -> `start * 1`: the inner
#   scan then compares row `start` to itself, always equal, and settles on the
#   same run end.
# - creditable_intervals `cursor < end` -> `<=`: a segment can never end past
#   the session end, so the extra iteration emits nothing.
# - intersect_intervals `left.1 < right.1` -> `<=`: at equal ends advancing
#   either list is the same; the other list's interval then produces an empty
#   overlap and is advanced on the next turn.
# - csv_parse `terminated.reserve(len + 1)` -> `* 1`, the `== b'\n'` row
#   estimate feeding `Vec::with_capacity`, and the `field_buf.len() * 2`
#   growth: allocation sizing only. The carried-bytes fix means any growth
#   step produces the same parsed value.
# - Loop-counter mutants that cargo-mutants already detects by timeout
#   (`+=` -> `*=` in the three interval/duplicate loops, `+=` -> `-=` on the
#   review-summary day cursor, `* 2` -> `/ 2` buffer shrink): detected, but
#   reported as a failed gate. Same precedent as the runtime exclusions above.
../rust/chronicle_preprocessing_semantic_adapter/Cargo.toml|wasm::
../rust/chronicle_preprocessing_runtime_wasm/Cargo.toml|src/lib\.rs:[0-9]+:1[37]: replace && with,replace \+= with .* in (physical_data_row_count|duplicate_safe_headers)
../rust/chronicle_semantic_index_wasm/Cargo.toml
../rust/chronicle_chrono_kernel_wasm/Cargo.toml|src/bin/,:20: replace > with >= in count_duplicate_groups,:12: replace > with >= in count_duplicate_groups,:70: replace - with / in unalign_duplicate_timestamps,:17: replace < with <= in unalign_duplicate_timestamps,:29: replace \+ with \* in unalign_duplicate_timestamps,replace \+= with \*= in unalign_duplicate_timestamps,:18: replace < with <= in creditable_intervals,replace \+= with \*= in creditable_intervals,:31: replace < with <= in intersect_intervals,replace \+= with \*= in intersect_intervals,src/pipeline_v2\.rs:4085:24: replace \+= with -= in build_review_summary,:44: replace \+ with \* in csv_parse,:60: replace == with != in csv_parse,:50: replace \* with (\+|/) in csv_parse|90|89|85|incremental-v2
../rust/chronicle_app_usage_matcher/Cargo.toml| in (to_py_error|match_app_usage(_update_(indices|arrays)|_arrays)?|split_overlapping_sessions_py|_rust_app_usage_matcher)|93|93|90||no-default-features
