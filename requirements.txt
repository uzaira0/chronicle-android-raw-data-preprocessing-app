polars>=1.17.0
pyarrow>=15.0.0
pytz>=2025.2
matplotlib>=3.10.7
numpy>=2.3.5
openpyxl>=3.1.5
pydantic>=2.0.0
PyQt6>=6.10.0
pyinstaller>=6.16.0
pytest>=9.0.1
# Transitive deps pinned to their security floors (matplotlib pulls pillow;
# pytest/rich pull pygments). osv-scanner resolves unpinned transitives to old
# vulnerable versions; these floors match what the runtime already installs.
pillow>=12.2.0
pygments>=2.20.0
