id: chronicle-web-rununit-confinement
snapshots:
  const out = await runUnit(parseEventsWiring, ctx, inputs);:
    labels:
    - source: runUnit(parseEventsWiring, ctx, inputs)
      style: primary
      start: 18
      end: 57
  runUnit(dedupWiring, ctx, { rows });:
    labels:
    - source: runUnit(dedupWiring, ctx, { rows })
      style: primary
      start: 0
      end: 35
