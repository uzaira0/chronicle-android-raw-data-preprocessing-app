export { CANONICAL_INTERACTION_TYPES } from "@/lib/generatedInteractionTypes";

/** Separator used to encode a `"Raw value => Canonical name"` remap entry (#4). */
export const INTERACTION_REMAP_DELIMITER = "=>";
